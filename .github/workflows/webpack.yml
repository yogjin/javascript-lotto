name: NodeJS with Webpack

on:
  push:
    branches: [step2]

jobs:
  deploy:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [14.x, 16.x, 18.x]

    steps:
    - uses: actions/checkout@v3

    - uses: actions/setup-node@v3
      with:
        node-version: 19

    - run: npm ci
    - run: num run build-step2
    
    - uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: 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
        publish_dir: ./dist
